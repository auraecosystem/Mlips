# Model Registry

This demo illustrates how you *could* promote a training run into the GitLab Model Registry:

- Create a model entry (once)
- Create a version per training run
- Attach parameters and README to the version
- (Optionally) attach artifacts / references

See `scripts/20_train_and_register.py`.
